/*To check puts() function*/
#include <stdio.h>

/**
 * main-Entry point
 * This program will print the velue of the variable using puts()
 * function and then add newline
 * Return: Always 0 (Success)
 */

int main(void)
{
	char Text[] = "\"Programming is like building a multilingual puzzle\n";

	puts(Text);
	return (0);
}
