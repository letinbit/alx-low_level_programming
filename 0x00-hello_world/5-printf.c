/* To test printf() function */
#include <stdio.h>

/**
 * main-Entry point
 *
 * This program wipp print what is written in printf function with newline
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
