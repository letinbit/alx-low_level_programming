/*Author is Neway Solomon*/
#include <stdlib.h>

/**
 * _abs - is a function that uses abs() function to return the
 * absolute value of passed argument.
 * @n: is a parameter that its absolute vale will be calculated
 * Return: the return value of abs()
 */
int _abs(int n)
{
	return (abs(n));
}
