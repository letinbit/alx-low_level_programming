/* Author is Neway Solomon */

/**
 * mul - is a function thar returns the multiplication of two numbers
 * @a: the first opperand for the multiplication
 * @b: the second operand for the multiplication
 * Return: aXb
 */
int mul(int a, int b)
{
	return (a * b);
}
